/**
 * DEPRECATED — Expo Router file.
 *
 * This project was migrated from Expo to a bare React Native CLI app.
 * Routing now lives in `navigation/RootTabs.tsx` (React Navigation) and the
 * entry point is `index.js` -> `App.tsx`.
 *
 * This `app/` directory is no longer used and can be safely deleted.
 */
export {};
